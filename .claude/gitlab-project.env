# Identité du projet GitLab associé à CE dépôt (framework inclus — voir
# CLAUDE.md > Réutilisation du framework pour un nouveau projet).
# Pas de secret ici, ce fichier est committé. À adapter dans chaque dépôt qui
# réutilise ce framework pour un nouveau projet.
GITLAB_PROJECT_PATH=ai-agent-projects/gitlab-facade
GITLAB_PROJECT_ID=84823522

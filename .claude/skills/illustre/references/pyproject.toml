[project]
name = "youtube-excalidraw-renderer"
version = "1.0.0"
description = "Renders Excalidraw diagrams to PNG for YouTube videos"
requires-python = ">=3.10"
dependencies = [
    "playwright>=1.40.0",
]

[tool.uv]
dev-dependencies = []
